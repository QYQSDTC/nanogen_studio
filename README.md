# NanoGen Studio 🍌

一个轻量级的 AI 图像生成工具，基于 Google Gemini 图像生成模型（Nano Banana）。

支持两种模型：
- **Gemini 3 Pro Image Preview** - 高质量，支持 1K/2K/4K 分辨率
- **Gemini 2.5 Flash Image** - 快速生成，适合高并发场景

## ✨ 特性

- 🎨 **现代化 UI** - 美观、直观的用户界面
- 🔑 **自定义 API** - 支持自定义 API Key 和 Endpoint
- 🤖 **双模型支持** - Pro 高质量模型 + Flash 快速模型
- 📐 **灵活参数** - 可调整纵横比和分辨率（1K/2K/4K）
- 🖼️ **参考图片** - 支持上传参考图片辅助生成
- 💾 **一键下载** - 生成的图片可直接下载
- 📱 **响应式设计** - 适配不同屏幕尺寸

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

应用将在 `http://localhost:3008` 运行。

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 📖 使用说明

### 1. 配置 API

在左侧边栏的 "API 设置" 部分：

- **API Key**: 输入你的 Google Gemini API Key（必填）
  - 获取 API Key: [Google AI Studio](https://makersuite.google.com/app/apikey)
- **Endpoint**: 可选，留空自动使用 Google 官方 API
  - 默认（留空）：使用 `https://generativelanguage.googleapis.com`
  - 自定义：只需输入基础 URL（如 `https://api.drqyq.com`）
  - 应用会自动补充路径：`/v1beta/models/{model}:generateContent`
- **模型**: 选择使用的模型
  - **Gemini 3 Pro Image Preview**: 高质量，支持 1K/2K/4K 分辨率
  - **Gemini 2.5 Flash Image**: 快速生成，固定 1024px

### 2. 设置生成参数

在 "生成参数" 部分：

- **纵横比**: 选择图片的宽高比
  - 1:1 (正方形)
  - 16:9 (横向)
  - 9:16 (竖向)
  - 4:3 (标准)
  - 3:4 (竖版标准)
  
- **分辨率**: 选择图片质量（仅 Pro 模型支持）
  - 1K (1024px)
  - 2K (2048px)
  - 4K (4096px)
  - *注意：Flash 模型固定使用 1024px*

### 3. 生成图片

1. 在 "提示词" 文本框中输入你想要生成的图片描述
2. （可选）点击 "上传参考图片" 按钮上传一张参考图片
3. 点击 "生成图片" 按钮
4. 等待生成完成，生成的图片会显示在下方网格中
5. 鼠标悬停在图片上可以看到下载按钮

## 🛠️ 技术栈

- **React** - UI 框架
- **Vite** - 构建工具
- **Tailwind CSS** - CSS 框架
- **Lucide React** - 图标库

## 📝 注意事项

- 需要有效的 Google Gemini API Key 才能使用
- API Key 仅存储在浏览器本地，不会上传到任何服务器
- 图片生成时间取决于网络状况和服务器响应
- 建议使用较新版本的 Chrome、Firefox 或 Safari 浏览器
- **模型**：默认使用 `gemini-3-pro-image-preview`，可切换至 `gemini-2.5-flash-image`
- **参数格式**：使用 `imageConfig.aspectRatio` 和 `imageConfig.imageSize`（遵循官方 API 规范）
- **官方文档**：[Gemini Image Generation API](https://ai.google.dev/gemini-api/docs/image-generation)

## 🎯 API 参考

本应用使用 Google Gemini API 的图像生成功能 (`gemini-3-pro-image-preview` 模型)。

### API 参数格式

```json
{
  "generationConfig": {
    "responseModalities": ["image"],
    "imageConfig": {
      "aspectRatio": "1:1",
      "imageSize": "2K"
    }
  }
}
```

**重要**：
- 参数必须包装在 `imageConfig` 对象中
- 使用驼峰命名法：`aspectRatio`（不是 `aspect_ratio`）
- 分辨率值：`"1K"`, `"2K"`, `"4K"`（大写）
- `imageSize` 参数仅适用于 Pro 模型

更多 API 信息请参考：

- [Gemini API 文档](https://ai.google.dev/docs)
- [API 参考](https://ai.google.dev/api/rest)

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

Made with ❤️ by NanoGen Studio

